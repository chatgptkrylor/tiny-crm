# T01: Scaffold MVC project, web.config, IIS site, git bare repo

- Risk: `high`
- Risk reasons: Release infrastructure + IIS + git setup; foundation every other ticket depends on
- Depends on: (none)
- Owns: `Shop.csproj`, `web.config`, `packages.config`, `Global.asax`, `App_Start/`, `Properties/`
- Specification hash: 3a794da9a07ae4c2148224791e33f1e1f12cba832ddef3cb2b8c0b81bfa60621
- Approval hash: 3a794da9a07ae4c2148224791e33f1e1f12cba832ddef3cb2b8c0b81bfa60621

## Acceptance criteria

- `Shop.csproj` targets `net47` with MVC 5 references.
- `web.config` has `<connectionStrings>` (SQL Express ShopCRM), `<sessionState mode="InProc" timeout="20" />`, `<customErrors mode="On" />`, `<authentication mode="None" />`.
- `packages.config` includes `BCrypt.Net-Next`.
- Bare git repo `Shop.git` exists on host; guest `C:\src\Shop` is a clone.
- IIS application `Shop` under Default Web Site points at `C:\src\Shop`, app pool .NET CLR v4.0 Integrated.
- Browsing `http://localhost/Shop` on the guest returns the MVC default home page (HTTP 200).

## Verification

```bash
# On host: verify bare repo
git --git-dir=/home/krylor/dev/yash/Shop.git rev-parse --is-bare-repository
# Expect: true

# Via WinRM on guest:
powershell -Command "Test-Path C:\\src\\Shop\\Shop.csproj"
powershell -Command "Test-Path C:\\src\\Shop\\web.config"
powershell -Command "(Invoke-WebRequest http://localhost/Shop -UseBasicParsing).StatusCode"
# Expect: True, True, 200
```