# T08: ReportsController + view with bars

- Risk: `normal`
- Risk reasons: Read-only aggregate query; no security boundary
- Depends on: T06
- Owns: `Controllers/ReportsController.cs`, `Views/Reports/`
- Specification hash: 3a794da9a07ae4c2148224791e33f1e1f12cba832ddef3cb2b8c0b81bfa60621
- Approval hash: 3a794da9a07ae4c2148224791e33f1e1f12cba832ddef3cb2b8c0b81bfa60621

## Acceptance criteria

- `ReportsController` (`[SessionAuth]`):
  - `GET /Reports` queries `SELECT Status, COUNT(*) FROM Customers GROUP BY Status`.
- `Views/Reports/Index.cshtml` renders:
  - Table (Status | Count).
  - Horizontal Bootstrap progress bars (width % = count/total*100) per status row.
- Report reflects actual customer data (matches seeded + any created/deleted customers).

## Verification

```powershell
# Via RDP/browser on guest (logged in):
# /Shop/Reports — table shows Lead/Contact/Customer rows with counts
# Bars render with correct proportional widths
# Counts match what /Shop/Customers list shows
```