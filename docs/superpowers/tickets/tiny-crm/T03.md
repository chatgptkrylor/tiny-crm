# T03: Repository layer + DbConnectionFactory + models

- Risk: `normal`
- Risk reasons: Pure data-access code; no security boundary; verified by compile + smoke queries
- Depends on: T02
- Owns: `Repository/`, `Infrastructure/DbConnectionFactory.cs`, `Models/`
- Specification hash: 3a794da9a07ae4c2148224791e33f1e1f12cba832ddef3cb2b8c0b81bfa60621
- Approval hash: 3a794da9a07ae4c2148224791e33f1e1f12cba832ddef3cb2b8c0b81bfa60621

## Acceptance criteria

- `Models/User.cs`, `Models/Customer.cs`, `Models/Interaction.cs` exist with properties matching the SQL schema.
- `Models/CustomerStatus` enum (Lead, Contact, Customer) and `Models/InteractionType` enum (Call, Email, Meeting, Note) exist.
- View models: `LoginViewModel`, `CustomerViewModel`, `InteractionViewModel` with DataAnnotations (`[Required]`, `[EmailAddress]`, `[StringLength]`).
- `Infrastructure/DbConnectionFactory.cs` reads connection string from `web.config` and returns `SqlConnection`.
- `Repository/IUserRepository.cs` + `UserRepository.cs` — `GetByUsername(username)`, `GetById(id)`.
- `Repository/ICustomerRepository.cs` + `CustomerRepository.cs` — `GetAll(page, pageSize)`, `GetById(id)`, `Create(customer)`, `Update(customer)`, `Delete(id)`, `GetCountByStatus()`.
- `Repository/IInteractionRepository.cs` + `InteractionRepository.cs` — `GetByCustomerId(customerId)`, `GetRecent(count)`, `Create(interaction)`.
- All SQL parameterized (no string concatenation).
- Project compiles with `msbuild /p:Configuration=Debug` on guest.

## Verification

```powershell
# Via WinRM on guest:
cd C:\src\Shop
msbuild /p:Configuration=Debug /v:m
# Expect: Build succeeded, 0 errors
```