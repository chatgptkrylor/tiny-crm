# T05: Shared layout + Bootstrap 4 + Dashboard controller/view

- Risk: `normal`
- Risk reasons: UI-only; no security boundary
- Depends on: T04
- Owns: `Views/Shared/_Layout.cshtml`, `Views/Shared/Error.cshtml`, `Controllers/DashboardController.cs`, `Views/Dashboard/`
- Specification hash: 3a794da9a07ae4c2148224791e33f1e1f12cba832ddef3cb2b8c0b81bfa60621
- Approval hash: 3a794da9a07ae4c2148224791e33f1e1f12cba832ddef3cb2b8c0b81bfa60621

## Acceptance criteria

- `Views/Shared/_Layout.cshtml` includes Bootstrap 4 via CDN, navbar with "Tiny CRM" branding, conditional links (Dashboard/Customers/Reports/Logout) shown only when `Session["Username"]` is set, `@RenderBody()`, footer.
- `Views/Shared/Error.cshtml` shows friendly message, no stack trace.
- `DashboardController` (`[SessionAuth]`):
  - `GET /Dashboard` queries total customer count, status counts, 5 most recent interactions (with customer name via JOIN).
- `Views/Dashboard/Index.cshtml` renders 3 stat cards + quick-access buttons.
- Browsing `/Dashboard` while logged in shows the metrics and seeded data.

## Verification

```powershell
# Via RDP/browser on guest (logged in as admin):
# Navigate to http://localhost/Shop/Dashboard
# Expect: total customers card shows >= 10, status counts visible, recent interactions list populated
```